/** Reserved height for the floating bottom tab bar on main tab routes. */
export const TAB_BAR_OFFSET = "5.5rem";

/** Viewport height minus bottom tab bar — use for main tab pages. */
export const TAB_PAGE_HEIGHT = `calc(100dvh - ${TAB_BAR_OFFSET})`;
